import 'package:flutter/material.dart';
import 'package:flutter/cupertino.dart';

class FeatureExampleWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text(
          'FeatureExampleWidget is working', 
        ),
      ),
    );
  }
}
