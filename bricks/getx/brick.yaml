name: getx_feature
description: Create a feature with GetX state management
version: 1.0.0

environment:
  mason: "0.1.0-dev.49"

vars:
  name:
    type: string
    description: Enter the feature name [lowercase only]
    default: feature
    prompt: Enter the feature name [lowercase only]